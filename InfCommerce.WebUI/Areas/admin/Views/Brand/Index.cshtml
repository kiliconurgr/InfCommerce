 @model IEnumerable<Brand>
 @{
    int i = 0;
}
<div class="right_col" role="main">
    <div class="">
        <div class="row" style="display: block;">
            <div class="col-md-12 col-sm-12  ">
                <div class="x_panel">
                    <div class="x_title">
                        <h2>Marka Yönetimi <span style="color:red">@TempData["hata"]</span></h2>
                        <a asp-action="Create" class="btn btn-round btn-primary float-right" style="color:#fff">Yeni Marka</a>
                        <div class="clearfix"></div>
                    </div>

                    <div class="x_content">
                        <div class="table-responsive">
                            <table class="table table-striped jambo_table bulk_action">
                                <thead>
                                    <tr class="headings">
                                        <th class="column-title">Marka Adı</th>
                                        <th></th>
                                    </tr>
                                </thead>

                                <tbody>
                                    @foreach (Brand b in Model)
                                    {
                                        <tr class="@(i%2==0?"even":"odd") pointer">
                                            <td style="vertical-align:middle"><a asp-action="Update" asp-route-id="@b.ID">@b.Name</a></td>
                                            <th><button onclick="deleteRecord('@b.Name','brand',@b.ID)" class="btn btn-round btn-danger float-right">Sil</button></th>
                                        </tr>
                                        i++;
                                    }
                                </tbody>
                            </table>
                        </div>


                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<!-- /page content -->